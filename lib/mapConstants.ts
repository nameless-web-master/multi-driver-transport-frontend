/** Stable empty array for `H3MapView` `selectedCells` — avoids invalidating memos. */
export const MAP_EMPTY_CELLS: string[] = [];

/** Map preview height on the new-order / zone-connection preview (px). */
export const ORDER_DRAFT_MAP_HEIGHT = 480;
